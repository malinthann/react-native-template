import * as React from 'react';
import {{properCase name}}Screen from './{{properCase name}}Screen';

interface Props {
}

interface State {
}

export default class {{properCase name}}Container extends React.Component<Props, State> {
    constructor(props: Props) {
        super(props);
        this.state = {
        };
    }

    render() {
        return (
            <{{properCase name}}Screen />
        );
    }
}
